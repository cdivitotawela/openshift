$TTL    604800
@	IN	SOA	{{ dns_domain }}. admin.{{ dns_domain }}. (
                  3
             604800
              86400
            2419200
             604800)

	IN 	NS	ns1.{{ dns_domain }}.

ns1.{{ dns_domain }}.	IN	A	{{ dns_network }}.{{ dns_ns1_id }}
{% for node in dns_hosts %}
{{ node.name }}.{{ dns_domain }}.   IN      A       {{ dns_network }}.{{ node.id }}
{% endfor %}
{{ dns_wildcard_name }}. IN  A    {{ dns_wildcard_ip }}
